function PageTitle({ children }) {
  return (
    <header className="bg-ksGreen text-white w-full h-16 text-4xl font-medium flex justify-center items-center ">
      {children}
    </header>
  );
}

export default PageTitle;
